{% load humanize_size %}Nouveau transfert partagé avec vous.

{{ sender_name }}{% if sender_email and sender_email != sender_name %} ({{ sender_email }}){% endif %} vous a transféré{% if transfer.title %} : {{ transfer.title }}{% endif %}.

{{ files|length }} {% if files|length == 1 %}élément{% else %}éléments{% endif %} · {{ total_size|humanize_size }}
{% for file in files %}- {{ file.filename }} · {{ file.size|humanize_size }}
{% endfor %}
Le lien est valable jusqu'au {{ expires_date }} à {{ expires_time }}.

Télécharger les fichiers : {{ download_url }}

—
Transferts — La Suite territoriale
